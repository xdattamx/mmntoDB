// Limpia el checkbox de la pestaña de admin  de las librerias para que no se imprima en los PDF
var toSet= "admin clean";
// set the field content 
 entry().set("Admin",toSet);
//Mensaje de aviso opcional
message(toSet);
